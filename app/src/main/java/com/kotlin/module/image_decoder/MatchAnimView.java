package com.kotlin.module.image_decoder;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

import androidx.annotation.Nullable;

public class MatchAnimView extends View {
    public MatchAnimView(Context context) {
        super(context);
    }

    public MatchAnimView(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }
}
